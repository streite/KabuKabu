<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/clvFirebasePopup"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageButton
        android:id="@+id/btnFirebasePopupWrite"
        android:layout_width="70dp"
        android:layout_height="100dp"
        android:adjustViewBounds="true"
        android:scaleType="fitCenter"
        android:padding="0dp"
        android:layout_margin="10dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="@+id/clvFirebasePopup"
        app:layout_constraintRight_toLeftOf="@+id/btnFirebasePopupRead"
        android:src="@drawable/firebase_upload"
        />

    <ImageButton
        android:id="@+id/btnFirebasePopupRead"
        android:layout_width="70dp"
        android:layout_height="100dp"
        android:adjustViewBounds="true"
        android:scaleType="fitCenter"
        android:padding="0dp"
        android:layout_margin="10dp"
        app:layout_constraintLeft_toRightOf="@+id/btnFirebasePopupWrite"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:src="@drawable/firebase_download"
        />

</androidx.constraintlayout.widget.ConstraintLayout>